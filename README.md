## Environment setup

Create a `.env` at the repo root (or load vars in your shell):

```
PORT=3000
SESSION_SECRET=dev-secret
MONGODB_URI=mongodb://127.0.0.1:27017/streaming_app
OMDB_API_KEY=your_omdb_api_key_here
```

Start the app:

```
npm install
npm run dev
```

# adorastream